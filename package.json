{
  "name": "dono-do-clube",
  "version": "1.0.0",
  "private": true,
  "description": "MVP de manager de futebol para web, pronto para GitHub + Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check src/db.js && node --check src/auth.js && node --check src/game.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}
